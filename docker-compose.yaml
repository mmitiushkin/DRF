version: '3.1'

services:
  db:
    image: postgres:13.5
    privileged: true
    restart: always
    ports:
      - 54325:5432
    environment:
      POSTGRES_USER: mitrosha
      POSTGRES_PASSWORD: password
      POSTGRES_DB: TODOnotes
